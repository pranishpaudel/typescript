function veronica(num) {
    return 2 + num;
}
veronica(5);
var loginUser = function (name, email, isPaid) {
    if (isPaid === void 0) { isPaid = false; }
    return 0;
};
loginUser("Lindas", "Insan", true);
var typeFunction = function (name) {
    return 'hi';
};
var heroes = [4, 2, 1];
heroes.map(function (hero) {
    return "hero is ".concat(hero);
});
function consoleError(errmsg) {
    console.log(errmsg);
}
